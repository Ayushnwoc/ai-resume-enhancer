import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // File size limit is handled in the API route
  // App Router handles file uploads via FormData
};

export default nextConfig;
